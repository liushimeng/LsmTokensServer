## 自动修复代码任务规范（LsmTokensServer）

> 本项目按 [MIT License](LICENSE) 开源。自动修复流程面向维护者，参与前请阅读 [CLAUDE.md](CLAUDE.md)。

### 顶层硬约束(不可违反)

1. **绝对禁止修改 `CLAUDE.md`、`AGENTS.md` 这两个规则文件。**
   - 两个文件是仓库的**项目规则唯一事实来源**(其中 `AGENTS.md` 是 Claude Code Agent 规范),任何 AI Agent 都不应在自动修复流程中追加、删除或修改其内容。
   - 修复摘要 / 版本基线 / commit hash / 教训只写到旁路文档(如 `TestReport/<BugID>_validation.md` 或对应 `docs/` 归档),**绝不写入规则文件**。
   - 如果发现某条结论「必须写进 `CLAUDE.md` 才有效」,说明规则设计有缺陷,应改 prompt 而非写规则文件。

2. **每一份被处理的报告文件都必须有明确归宿**(判定规则见「执行流程」§5):
   - **A. 有问题已修复并推送成功** → **删除**该报告文件,不保留「历史归档」。
   - **B. 经核查无问题**(报告无有效缺陷、未改代码、无新提交)→ **就地重命名**,追加 `_无问题` 后缀;不做删除、不做提交以外的其他处理。
   - **C. 有问题但构建/重启/验证失败** → **保留原文件名不变**,等待人工介入。
   - `TestReport/` 与 `go-web-debug-tool/UseReport/` 中**只要存在**未带 `_无问题` 后缀的 `自动化测试报告_*.md` / `协议抓包分析报告_*.md` / `测试工具使用报告_*.md`,就视为待处理;不得因积压跳过。

3. **LsmTokensServer 工程约束**([CLAUDE.md](CLAUDE.md)):
   - 编译 / 启动 **必须走脚本** `./rebuild_restart_app.sh`,禁止直接 `go build` / `nohup ./LsmTokensServer`。
   - 端口规范:管理端 `9101`、AI 代理 `29000`/`29003`、用户端 `29001`、MCP `29002`、爬虫 CDP `9222`。修复如涉及端口/配置,必须与 `LsmTokensServer.conf.example` 一致。
   - 前端**双构建隔离**:管理员 Web(`ClientWeb/dist-manager`)与用户 Web(`ClientWeb/dist-user`)禁止共享目录或跨目录回落;`__APP_ROLE__` 为构建期常量;用户端产物零管理代码。
   - 安全红线:JWT 密钥与管理端登录凭证只放 `LsmTokensServer.conf` 的 `security` 段;用户密码只存 bcrypt 哈希;接口响应禁止明文密码/完整手机号;前端禁止持久化 API Key。
   - 敏感文件(`.conf` / `*.crt` / `*.key` / `.env` / `tmpPlan/` / `user_model_info.json`)**严禁提交**。

### 执行指令(自动启动)

> 本 prompt 由 `AutoDebugTestReport.sh` 加载,调用方**已授权**你直接执行,无需进一步确认。

- **收到本 prompt 立即进入「执行流程」§1「报告检索」**。
- **不要输出「请确认是否启动」/「是否立即开始」等确认性问句**——已废弃反模式,会触发 Agent 走意图确认路径直接退出。
- **不要询问用户偏好、模型选择、是否推送远程**——按本规范默认值执行;遇真正需要决策的歧义,写入 `TestReport/<BugID>_questions.md` 由人工后续处理,继续往下推进。
- **每完成一步必须继续推进**,直到所有流程结束或触发 25 分钟硬上限。

### 执行流程

#### §1 报告检索(≤ 2 min)
- **主工程**: 在 `TestReport/` 下检索**生成时间最早、文件名不含 `_无问题` 后缀**的报告。
  - 当前报告 glob：
    - `自动化测试报告_*.md`（管理员/用户 Web 功能测试）
    - `协议抓包分析报告_*.md`（Anthropic↔OpenAI 协议适配）
  - 任一 glob 命中即视为有待处理报告。
- **子工程**: 在 `go-web-debug-tool/UseReport/` 下检索**生成时间最早、文件名不含 `_无问题` 后缀**的报告
  (`测试工具使用报告_*.md`)。
- 两类报告可并行检索;无任何待处理报告 → 静默结束。

#### §2 缺陷分析与代码修复(≤ 12 min)
- 按报告 `Bug ID` + 堆栈 + `ERROR` 日志,在对应代码库定位问题。
  - 后端: `ServerGo/` 下按业务域分包(`api/` / `config/` / `database/` / `models/` / `proxy/` / `protocol/` / `recognizer/` / `spider/` / `system/` / `websocket/` / `webserver/`)。
  - 前端: `ClientWeb/src/pages/` + `ClientWeb/src/shared/`,双构建隔离。
- **动手改代码前必须先确认缺陷当前是否仍然存在**:`git log --oneline -10 -- <file>` 看相关文件近期提交、比对当前实现与报告中堆栈,避免对已修复缺陷重复打补丁。
- **根因分析**: 从逻辑底层修复,避免仅处理表面;核心逻辑(如 WebSocket 重连、JWT 鉴权、AI 协议转换、路由选择算法)遵循 `CLAUDE.md` 架构约束(仅**读取**作参考,绝不修改)。
- 核查后确认问题**均已不存在或非有效缺陷** → 不改任何代码,直跳 §5 分支 B。
- 修复后必跑:
  - **Go 后端**:`go build ./...` + `go test ./...` 全绿。
  - **前端**:`npm run build` 双构建(`dist-manager` + `dist-user`)均通过;构建后 grep 复验 `dist-user` 不含任何管理端字样(`UserManage`/`ManagerLogin`/`UserManageInterface`/`ManagerLoginInterface`)。

#### §3 构建重启与回归验证(≤ 8 min)
- 主工程跑 `./rebuild_restart_app.sh` 重新编译 + 重启服务(必须走脚本,禁止裸 `go build` + `nohup`)。
- `go-web-debug-tool` 子工程独立编译验证。
- 回归测试:
  - 管理端(`9101`)核心页面可用性(首页/用户管理/模型信息/对话分析)。
  - 用户端(`29001`)登录 + 首页 + 对话分析。
  - AI 代理(`29000`)协议转换冒烟(如报告涉及)。
- **本步骤仅在「实际改了代码」时执行**;分支 B 跳过。

#### §4 文档同步(仅旁路,≤ 2 min)
- 修复涉及架构调整 / API 变更 / 新增常量 / 端口配置 → **只**更新业务/技术文档(`docs/*.md`)与代码内注释。
- **禁止**追加、删除、修改 `CLAUDE.md` / `AGENTS.md`。
- 修复摘要 / commit hash / 回归结果可写 `TestReport/<BugID>_validation.md` 作为本轮验证记录。
- **本步骤仅在「实际改了代码」时执行**;分支 B 跳过。

#### §5 Git 提交推送与报告归档(≤ 1 min)

按本轮实际结果,**三选一**走对应分支:

**分支 A: 有问题已修复并验证通过**
- 主工程 + `go-web-debug-tool` 子工程分别 Git 提交,注释统一:
  `修复: [BugID] <问题简述> (关联报告: YYYYMMDD_HHMMSS)`
  示例: `修复: BUG-PROTO-SSE Anthropic SSE 流截断问题 (关联报告: 20260826_143022)`
- commit message 使用中文,格式 `阶段X: 简要说明`。
- 提交成功后自动 `git push` 同步远程。
- **推送完成后立即删除**已处理的对应报告文件(主工程 `TestReport/`,子工程 `UseReport/`);原始报告不应在仓库长期堆积。如需保留修复摘要,迁移到 `docs/` 下按主题归档,**不要让原始报告留存**。

**分支 B: 无问题(无需改代码、无新提交)**
- 判定条件**同时满足**: 问题经核查不成立或早已修复、本轮**未修改任何代码**、**无新 Git 提交**。
- 处理方式: 报告文件**就地重命名**,追加 `_无问题` 后缀:
  - 主工程: `TestReport/自动化测试报告_YYYYMMDD_HHMMSS.md` → `..._无问题.md`
  - 子工程: `go-web-debug-tool/UseReport/测试工具使用报告_YYYYMMDD_HHMMSS.md` → `..._无问题.md`
- 重命名后该文件**不再被后续轮次检索**。
- 本分支**不删除文件、不代码提交推送**;若仓库对该目录做版本管理,仅允许提交这一次重命名,注释:
  `处理: 测试报告无问题归档 (关联报告: YYYYMMDD_HHMMSS)`
- 可在文件末尾追加一行简短结论(如「经核查无有效缺陷,无需修改代码」),便于人工回看。

**分支 C: 有问题但构建/重启/验证失败**
- **保留原报告文件名不变,不得删除、不得重命名**。
- 按「约束与监控」记录错误日志,等待人工介入。

### 约束与监控

- **总时长硬上限 25 分钟**: 超时强制终止当前任务并发告警,防多任务积压;超时终止时报告文件**保留原名**,不做删除或重命名。
- **子阶段时间盒**: 检索 ≤ 2 min / 修复 ≤ 12 min / 构建 ≤ 8 min / 文档 ≤ 2 min / 归档 ≤ 1 min,任一阶段逼近上限应优先保核心动作(改代码 → 验证 → 归档),次要动作(旁路文档细化)可简化。
- **错误处理**: 构建/重启/验证阶段失败,记录错误日志,报告文件**保留原名**,**不得自动删除,也不得标「无问题」**,等人工排查。
- **规则文件守门**: 流程中任何「更新 `CLAUDE.md`」冲动,先停下来确认——本流程明确禁止;确有规则层面变更需求,由人工评审后单独提交,不纳入自动修复范围。
- **Git 推送权限**: 默认 `git push` 同步远程;若远程不可达,记录错误,本轮走分支 C(报告保留),等人工介入。
- **数据安全**: 抓包数据严禁明文输出 API Key / Token / Cookie / 密码,必须脱敏。
